{
  "name": "gooqi-medical-transcriber",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "preflight": "node scripts/preflight.mjs",
    "clean": "turbo run clean && node -e \"fs.rmSync('node_modules', { recursive: true, force: true })\""
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.23.0",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
